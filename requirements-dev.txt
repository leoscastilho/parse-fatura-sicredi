# Só para rodar a suíte. Não vai para a imagem final.
-r requirements.txt

pytest==9.1.1

# O `starlette.testclient` precisa de um cliente HTTP, e a partir do Starlette
# 1.0 esse cliente é o httpx2. Sem declarar aqui, o build falha com
# "The starlette.testclient module requires the httpx2 package to be installed"
# — mas só na máquina onde ele não estivesse instalado por acaso, que foi
# exatamente como esse bug passou despercebido.
httpx2==2.12.0

# Escreve `.xls` (BIFF), que o openpyxl não escreve. Só a suíte usa, e para uma
# coisa que nenhum `.xlsx` consegue provar: um `.xls` é um container OLE2, o
# mesmo de um `.xlsx` protegido por senha. Sem um arquivo desses, "isto está
# cifrado?" nunca era perguntado a um OLE2 aberto — e a resposta errada faria o
# portal pedir senha para toda fatura do Sicredi baixada do site.
xlwt==1.3.0
